#!/bin/bash 

cd /opt

if [[ "$1" == -a ]]; then
  /opt/tiosetup/run-scripts /opt/tiosetup/languages
else
  if [[ "$1" ]]; then
    /opt/tiosetup/languages/$1
  else
    echo 'Missing parameter. Usage: tiopull language|-a'
  fi
fi
